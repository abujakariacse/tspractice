{
  // Basic data types

  // string
  let firstName: string = "Jakaria";

  // number
  let roll: number = 38;

  // boolean
  let isAdmin: boolean = true;

  // undefined
  let x: undefined = undefined;

  // null

  let y: null = null;

  // Array

  let friends: string[] = ["jack", "mack", "rack"];

  let rollList: number[] = [1, 3, 4, 5, 6];

  // touple -> maintain order of types

  let nameRollSmoker: [string, number, boolean] = ["Jakaria", 13, true];
}
